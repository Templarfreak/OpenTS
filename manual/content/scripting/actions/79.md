---
type: action
id: TACTION_SET_TEAM_UNIT_RATIO
title: Ratio of team units...
summary: Stores a vehicle share on the trigger's house that nothing reads.
valid_values:
  - Any number. Every value behaves the same way, because nothing reads what is stored.
caveats:
  - The action runs and does nothing beyond the store. Nothing that decides what a house produces reads the share back, so nothing about what a house builds for its teams changes whatever is written.
  - The house written to is the one that owns the trigger. No house is named on the line.
  - Of the four ratio actions only the AI-trigger one, which shares the naming, reaches a decision. Its two companions here are inert in the same way this one is.
related:
  - type: key
    id: RatioTeamUnits
  - type: action
    id: TACTION_SET_TEAM_AIRCRAFT_RATIO
  - type: action
    id: TACTION_SET_TEAM_INFANTRY_RATIO
  - type: action
    id: TACTION_SET_AI_TRIGGER_TEAM_RATIO
  - type: system
    id: ai-team-production
---
