---
type: action
id: TACTION_SELL_ATTACHED
title: Sell building
summary: Sells every structure whose tag is attached to this trigger, through the ordinary deconstruction.
caveats:
  - The action sweeps every structure in the scenario and sells each one whose tag is attached to the same trigger; it is not limited to the structure that sprang it.
  - A structure already deconstructing is passed over, and so is one with a demolition charge counting down on it.
  - Neither the type's own refusal to be sold nor the sellable field on the scenario's structure record is read, so both routes that keep a structure out of the player's hands leave this one open.
  - A firestorm wall section is not deconstructed at all. It is taken off the map on the spot, whether or not its house's defense is raised.
related:
  - type: key
    id: Unsellable
  - type: key
    id: Buildup
  - type: key
    id: RefundPercent
  - type: system
    id: laser-fences
  - type: action
    id: TACTION_DESTROY_OBJECT
---

## What is sold

The action walks every structure in the scenario and sells each one that clears all of these, in this order:

1. It is active.
2. It has been put down on the map.
3. It is not in [limbo](/glossary/#limbo).
4. It has a tag.
5. That tag is attached to this trigger.

Each one that clears them is put on the deconstruct mission, which plays the construction animation in reverse; one firing can therefore sell several separately tagged structures at once. The sale refunds [`RefundPercent`](/keys/refundpercent/) of the price to a house a human plays, and the full price to a computer house.

Two of the refusals a player meets do not apply here. [`Unsellable=yes`](/keys/unsellable/) is tested by the sell cursor and not by the sale itself. The sellable field a scenario writes against each structure it places is not read either. It reaches only [the computer's decision to sell a building it cannot afford to repair](/systems/repair/#when-the-computer-repairs). The refusal that does apply is the missing construction animation, since [a type with no buildup artwork cannot be sold](/keys/buildup/#a-type-with-no-construction-artwork-cannot-be-sold) by any route, this one included.

A [firestorm wall](/systems/laser-fences/#the-firestorm-wall) section has no buildup artwork of its own either, so it is not sold. It is taken off the map on the spot, and the removal returns nothing. The sell cursor offers that removal only while the house's defense is down; this action makes no such test.
