---
type: action
id: TACTION_ADD_TIMER
title: Timer Extend...
summary: Adds seconds to the mission countdown and sets it running.
valid_values:
  - A number of seconds. The figure is multiplied by the frame rate of fifteen and added to the count.
caveats:
  - The new count is the time already left plus the seconds added, and the result is a running countdown, so extending a stopped countdown starts it counting again.
  - Nothing is announced, and the clock simply jumps to the new count.
  - The spoken reminders are given as the count passes exactly twenty, ten, five, four, three, two and one minutes, so an extension that steps over one of those marks skips its reminder.
related:
  - type: action
    id: TACTION_SUB_TIMER
  - type: action
    id: TACTION_SET_TIMER
  - type: action
    id: TACTION_STOP_TIMER
  - type: system
    id: sidebar
---
