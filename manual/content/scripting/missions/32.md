---
type: mission
id: TMISSION_ION_STORM_START
title: "Ion storm start in..."
summary: "Starts an ion storm of the given length, unless one is already running."
caveats:
  - "The number is passed through as game frames. The trigger action of the same name multiplies its own number by the frame rate first, so the same figure means fifteen times as long there."
  - "The line is skipped while a storm is running, and the team still takes the next line."
  - "The line does not hold the team: it runs once and the team takes the next line of its script on the following turn."
related:
  - type: action
    id: TACTION_ION_STORM_START
  - type: system
    id: ion-storms
---
